from manim import *

# camera related
VIEW_INTRO = {
    'phi': 60 * DEGREES,
    'theta': -75 * DEGREES,
}
VIEW_COMPUTE = {
    'phi': 60 * DEGREES,
    'theta': -155 * DEGREES,
}

# cube related
SMALL_CUBE_SIZE = 0.3
MEDIUM_CUBE_SIZE = 0.5
SMALL_FONT_SIZE = 10
MEDIUM_FONT_SIZE = 18
